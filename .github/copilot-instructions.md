# Copilot Chat Instructions — PhotoViewer

> 项目架构、目录导航、数据流等完整说明见 **`AGENTS.md`**（Agent 模式的主文档）。

## 编码风格与规范

- **注释**：在每个函数前使用 XML 注释（`/// <summary>...</summary>`）说明功能、参数、返回值。对于复杂逻辑，添加行内注释。使用中文。
- **复用**：优先复用现有函数和组件，避免重复代码。新功能新组件应规范预留可复用可扩展性。
