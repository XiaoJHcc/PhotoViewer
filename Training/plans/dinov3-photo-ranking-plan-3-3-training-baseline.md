# DINOv3 照片美学评分 — Plan-3-3 训练侧：基线 + 绝对涌现测量（M4 + M5 条件分支）

> 状态：**契约已锁 v1.0 / 2026-07-05 · 方案未开工**——§1–§5 为锁定契约（§4 失败映射亦已锁定）；§6 想法**均未锁定**，开工时按前序真实产出重定。
> 上游宪法：[Plan-3-0](dinov3-photo-ranking-plan-3-0-charter.md) · 前序：[Plan-3-1](dinov3-photo-ranking-plan-3-1-data-foundation.md)（M1）→ [Plan-3-2](dinov3-photo-ranking-plan-3-2-calibration-pairs.md)（M2+M3）
> **开工条件**：plan-3-2 两道 GATE 通过（训练对数据集 + 绝对星级 + split 就绪）；金标准集**必须已可用**——它是绝对涌现的裁判。

---

## 1. 绑定的宪法决策（plan-3-0 §1，本册必须落实）

决策 **4**（软偏好损失、不做对星级的绝对回归）、**6**（v1 纯单图 → score，邻域聚合条件触发）、**7**（架构定型前线性探针——M1 已完成，本册直接消费结论）、**8**（backbone 升级梯四级封顶，LoRA 兜底）、**9**（置信度走事后估计，不训伪造目标的置信度头）、**10**（评估两条线）、**13**（输入制式承 M1 §1.2 结论）。

## 2. 本册必须回答的问题（决策清单）

1. 损失形式：pairwise vs listwise（候选组数据两者可派生，实测定）；
2. 架构起点超参：轻量 MLP 的维度 / 层数 / 正则（起点结构见 §6.1 迁入想法）；
3. 训练细节：采样策略（组内采样 vs 全局打散）、可信度权重接入方式（loss 加权 vs 采样加权）；
4. 若 M2 选了 b_seg 路线：b_seg 的初始化（人工偏移值）与正则（锚点约束强度）；
5. **a/b/c 决策点**：绝对涌现测量 → **a** 够（进 plan-3-4）/ **b** 不够（触发 M5）/ **c** 特征不行（回决策 8 升级梯）；
6. M5（条件触发）：偏移监督加码幅度、targeted 跨段标注的量与选点策略；
7. 置信度估计器选型：窗口间打分方差 / 到决策边界距离 / 邻居分歧，择一或组合。

## 3. 输入 / 产物 / GATE

- **输入**：plan-3-2 产物（训练对 + 权重、绝对星级、split）、M1 制式结论（单 / 多视图 CLS）、金标准集。
- **产物**：基线模型 + 训练脚本；绝对涌现测量报告（好/坏天气片落位、锚点是否落顶端）；a/b/c 决策记录；置信度估计方案；（若触发 M5）增强后复测报告。
- **GATE（M4）**：段内 ranking 达标（段内 top-1 命中、段内 0-5 序相关）→ 地基成立，再按金标准判绝对涌现 a/b/c；**段内都不达标 → 按 §4 失败映射查表分诊，不进 a/b/c**。
- **GATE（M5，条件）**：复测绝对涌现有实质改善 → 进 plan-3-4；仍不够 → **降级保底交付形态**（plan-3-0 §0.6），绝对分留 v2，照常进 plan-3-4 集成。

## 4. 失败模式 → 对策映射（**已锁定**，自 plan-3-1 §6.3 迁入）

> 把 M4 从"看结果再想办法"变成"查表执行"：按七类场景（plan-3-0 §0.5）的验收结果分诊，各失败模式对应的病灶层次不同，对策互不混淆。

| 挂掉的场景 | 诊断（病灶层次） | 对策 |
|---|---|---|
| 场景 2（长焦梯田细节判别） | 特征分辨率不足 | 升 ViT-L 重提（plan-3-0 决策 8 升级梯），**不动架构** |
| 场景 4 / 7（像素级差异、局部抖动定位） | patch 空间结构在聚合中丢失 | 架构升空间变体（保留 patch 空间信息，§6.1 预案），**不动特征** |
| 场景 5（孤立氛围片绝对判断） | 跨段绝对信号不足 | 走 M5 校准增强（加偏移监督 / targeted 跨段标注），**不动架构不动特征** |
| 场景 1 / 3（构图取舍、差异部分优劣） | 训练对质量 / 可信度权重问题 | 先查配对生成与权重映射（plan-3-2 产物），最后才怀疑特征 |
| 场景 6（题材化权衡不切换） | patch 语义权重未起作用 | 查三路融合里 patch→CV 加权是否退化（§6.1），属架构内调整 |
| 全场景普遍平庸 | 特征可分性基础问题 | 回 plan-3-0 决策 8 升级梯逐级排查（LoRA 封顶） |

## 5. 进册必读（上下文恢复索引）

plan-3-0 §0.5（七类场景）、§0.6（目标 / 三大挑战 / 保底形态 / 成功判据）、§1 决策 4 / 6-10 / 13；plan-3-2 产物报告全部；plan-3-1 §1.2 制式结论、§1.5 绝对性探针结论；金标准集标注说明。

## 6. 初期想法（**均未锁定**）

> 自 plan-3-1 §6.2 迁入（2026-07-05）。

### 6.1 单图编码器架构起点

> 承 plan-3-0 §1 决策 6（纯单图）、决策 7（先线性探针）。具体维度 / 层数待 M1 探针 + audit 后定。

- **三路融合的核心概念**（来自一 / 二期）：**patch token 作为 CV 网格的 per-cell 语义权重**——每格"该不该锐"由 patch 语义定（主体格重、背景格轻），每格"实际多锐 / 多抖"由 CV 物理量给。这正是存 patch token 的理由，对应场景 6（题材化权衡）。**不**在架构里硬编码"主体清晰度权重 > 背景"，让 MLP 自己学。
- **单图编码起点**（轻量优先、防过拟合）：CLS(384) + CV 网格按 patch 主体权重做加权统计（每标量的加权 mean / std / 分位 + 最差格）+ EXIF 向量 → 小 MLP → score。先跑这个轻量版；若失败集中在"局部抖动 / 虚焦"（空间信息丢失）再上保留更多空间结构的变体。
- **EXIF 向量化**：对数化再标准化（log 焦距×crop、log 光圈×crop、log 快门、crop），缺失填 0 + mask 位；约 4-8 维。
- **判断优先级的架构含义**（plan-3-0 §0.6）：构图 → 内容 → 光影；构图 / 空间结构更依赖 patch 的空间分布而非纯 CLS，编码器要保留足够空间信息。
