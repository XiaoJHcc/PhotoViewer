# DINOv3 照片美学评分 — Plan-3-1 数据地基（M1）

> 上游宪法：[Plan-3-0](dinov3-photo-ranking-plan-3-0-charter.md)（前提 / 意图 / 已锁定技术决策）
> 本期任务：**M1 — 数据地基 + 特征可行性**。这是整个三期的地基：先验证特征、把数据备齐、看清真实分布，再谈训练。**M1 没跑完不锁 M2 起的细节。**
>
> **当前推进**：以**照片增强制式探针**为第一步（见 §1.2）——它决定全量入库的输入制式，必须在入库前定。本期把增强算法的**探索与目视评估搬进产品**（查看器控制栏一个 toggle，原地预览增强 / 还原），人机共用同一确定性算法；Python 侧只保留量化判据（线性探针 ≥80%）。
>
> **进度真源**：本文件只记计划、不追进度；当前里程碑位置 / 最近 GATE / 下一步见 [../STATUS.md](../STATUS.md)，实验证据链见 [../EXECUTION-LOG.md](../EXECUTION-LOG.md)。
>
> 原则（承 plan-3-0 §2）：每步带 **GATE**，不达标就停下排查 / 回退，不假设一路顺利。

---

## 0. 为什么 M1 必须先行、且独立成期

- 后续一切（配对、绝对校准、训练、评估）都依赖 M1 的产物与 audit 结论；M1 的真实数据分布出来前，M2+ 的阈值、配对量、绝对涌现都是猜测。
- 几个"开工前必须落实"的条件只有 M1 能确认：**星级可读性、输入增强制式、ViT-S 特征是否够敏锐、磁盘 / 覆盖率**。任一不过，方案要改。
- 所以 M1 自成一期、带硬 GATE；跑完回看 plan-3-0 §2，再列 plan-3-2（M2 起）。

---

## 1. M1 步骤与 GATE

### 1.1 数据可读性前置确认（快速闸，部分已确认）

历史星级是这套训练数据的标签来源，开工前确认能读到。

- **已确认**：指纹聚合可靠（同次曝光 RAW/HEIF/JPG 合一）、各题材库与"金标准"的相对配比大致对应（plan-3-0 §0.4 / §1 决策 11）。
- **仍需抽样确认**：人工星级确实写在 **XMP / 同名 `.xmp` sidecar** 上、能被读到（ARW 的 rating 通常在 sidecar，不在文件内）；拍摄时间字段齐全（`DateTimeOriginal` / `SubSecTimeOriginal`，指纹与拍摄段切分都依赖）。用 `Tools/ExifTestTool` 抽查，**重点覆盖老批次**（跨年历史文件可能改名 / 缺 SubSec / sidecar 与文件时间不一致）。
- **仍需盘点：精修成品回溯匹配**（承 plan-3-0 §0.4 前提 / §1 决策 3b）：盘点历年精修导出件的存放位置与数量，确定回溯到直出原片指纹的匹配规则——导出件文件名 / 尺寸 / EXIF 可能已变、指纹大概率直接对不上，需按拍摄时间 + 原文件名线索匹配并抽样人工核验。匹配结果是 1.3 入库 `is_retouched` 标记的唯一来源。
- **顺手确认**：后续外拍**保留原始 dump**（含将被删掉的废片）——它是废片闸门（plan-3-0 §1 决策 14）与金标准废片组（§5）唯一的数据来源，错过一次外拍就再等一次。
- **GATE**：星级读不到（只在 Lightroom / Capture One 的 catalog 里）→ **先批量导出 XMP / sidecar**，否则后续全部无从谈起。精修回溯匹配不可行 → 不阻塞 M1 其余步骤，但决策 3(b) 信号降级，绝对校准的腿在 plan-3-2 重议。

### 1.2 照片增强制式探针 + 特征可行性（**当前要执行的第一步**）

> 这一步合并两件事：① 选定喂 DINO 的**输入制式**（仅原片 / 仅增强 / 多视图）——它决定 1.3 入什么、特征意味着什么，必须在全量入库前定；② 同时验证 backbone 特征对"段内细差"够不够敏锐（原线性探针）。依据见 plan-3-0 §0.3（雾 / 低对比片标签建立在 ACR 增强视图上）、§0.7、§1 决策 13。

**已确定（本次讨论收敛，记录在此、不再反复）：**

1. **目标是"兼得"**：构图可辨（尤其雾 / 低对比片，记为 **A**）+ 影调理解（天生好影调要认得出，记为 **B**）。实现机制 = **多视图输入**（原片 + 确定性增强，两路 CLS 拼接）：原片路保 B、增强路补 A。
2. **不用**不变性增广（会教模型忽略影调 → 杀 B）、**不用**调色版当更高星新样本（OOD）。
3. **铁约束**：增强必须是**确定性算法、推理期对每张可复现**；**手动 ACR 修图只作调参参照、不作输入**（production 里不可能每张手修——精修像素同理，见 plan-3-0 §0.4）。
4. **判据 = DINO 判别力（线性探针准确率），不是主观好看**；普通可比对集里要**专门含"靠影调 / 氛围区分"的对**，否则测不出"仅增强"有没有伤 B。
5. **增强产品化、算法探索搬进产品**（落地 plan-3-0 §1 决策 13 末句"产品化待本节"）：增强既是模型输入视图、又是查看器产品功能，二者共用同一确定性算法；算法的探索与目视评估在产品内做，而非纯 Python 脚本里盲调。

**产品化落地（算法探索的载体）：**

- **入口**：图片查看器**控制栏新增一个 toggle 按钮**，点击即把主画面原片**原地替换**成增强版本，再点还原。落点在 `ViewModels/Main` 的控制 VM（`ControlViewModel`）+ 图片 VM（`ImageViewModel`），增强是一步**确定性图像处理**（输入原片解码位图 → 输出增强位图），不入库、不动原文件。
- **已落地算法（本次收敛，覆盖原"v0 粗暴均衡→逐步换 CLAHE"设想）**：定位是**信息归一化器**——不追求好看成片，而是把有效信息分布得更均匀、对比更清晰但**低失真**，供模型 / 人眼判断"后期潜力"（真正后期由人做）。技术选型 = **全局限对比直方图均衡（CLHE）**：亮度直方图按 `ClipFactor` 裁剪封顶 + 均匀回填（封住 LUT 斜率 → 防断层、防大面积同色尖峰独吞输出范围）→ cdfMin 归一化 LUT → **保色度重建** `ch' = Y' + s·(ch − Y)`（YCbCr 恒定色度，`s = SaturationScale`）。该式结果亮度恒等于 `LUT[Y]`（彩度向量在亮度方向投影为 0）→ 亮暗关系严格单调、色相不变；`s=1` 保**绝对**彩度，暗部不随亮度塌缩发灰。落点 `Core/Image/ImageEnhancer.cs`，纯算法、不入库、不动原文件。
- **为什么是 CLHE 而非局部 / 分段 / 自适应**：选型第一约束是**可学习性 5 规格——全局、单调、平滑(斜率有界)、少量固定参数、色彩稳定**，使"原片↔增强片"是一致的全局单调色调变换、模型能建立关联。据此**显式排除**真分块 CLAHE（局部自适应 → 同输入随位置出不同值）、双直方图分段（拐点 / 切点逐图跳）、逐图自适应强度（再叠一层逐图变化）——它们都更"漂"、伤可学习性；裁剪一步即覆盖"匀信息但不过度摊平"的目标。
- **可调参数（固定常量，不进设置）**：`ClipFactor = 2.0` 是唯一手感旋钮 = **保守↔激进**：→1 近线性最稳、↑ 对比更强、≳4 退回裸 HE（断层 / 漂移回潮），实用区间 **1.5–3**；`SaturationScale = 1.0` = 色度缩放（1 保绝对彩度＝饱和度中性、>1 更艳、<1 回退发灰）。
- **色彩重建为何从"等比增益"改"恒定色度"**：旧式 `ch = ch·(newY+ε)/(oldY+ε)` 精确保住的是**相对**饱和度（HSV `S`），但绝对彩度 ≈ 亮度 × 相对饱和度，CLHE 压暗暗部后彩度随亮度一起趋零 → 暗部发灰发黑（雾片树几乎全黑）。改保**绝对**彩度 `ch' = Y' + s·(ch − Y)`：暗部彩度不随亮度塌缩、亮暗关系仍严格单调、无除法故取消 ε（深阴影不再放大彩噪）。
- **目视评估（本次结论）**：雾天灰片显著增强（人眼 / DINO / CV 提取均更足）；四类直方图（中部单峰 / 极灰窄峰 / 偏态 / 双峰）均表现良好，无断层、无亮度漂移、无暗部彩噪。
- **铁约束（承 plan-3-0 §1 决策 13，不变）**：CLHE 是**产品 / 目视侧已收敛**的信息归一化算法，也是模型输入的**当前首选候选**；但是否采纳为模型输入仍受下面**线性探针 ≥80% GATE + 制式决策**约束；产品 ship 版与模型输入版须收敛到**同一非破坏性算法**。

**本次目视验证 + 入模前提（统一认知，本次讨论收敛）：**

- **A（构图可辨）目视已坐实**：DINO 诊断图上，原本埋在雾里的远处桥 / 建筑，增强后与海面、山体分离开 → 增强对 A 有实效。但这是**先行指标**（patch 级、近 N=1 目视），不等于探针达标（CLS 级、定量 ≥80%）。
- **B（影调理解）目视沉默、仍待探针**：桥分离只证 A，对"会不会把天生好影调与被救回来的灰片抹平"这条 B 风险一字未答；探针样本须**专含影调对**才测得出。
- **产品价值 ≠ 模型价值，二者可分离**：增强对**人眼选片**的价值基本坐实（作为控制栏 toggle 留用，与是否入模无关）；增强对**模型**有无增量是独立问题，可能结论是"原片已够"。
- **制式（架构倾向 vs 数据出口）**：架构上倾向**原片 + 增强多视图**（保 B 补 A）；但**保留本节既定出口"仅原片在低对比上已够分 → 不增强、收工"**——增强这一路是否真要入模，由探针 ≥80% + 双视图成本 / 收益（双 DINO pass、双存储、端侧成本）判，不预设。
- **入模前一致性清单（执行项，非决策）**：① C# 产品侧与 Python 探针 / 入库**按容差对齐**（两侧解码器不同，字面逐像素达不到；判据 = 同一原始文件增强后亮度差 |ΔY| ≤ 1，超差先排查解码 / 缩放差异）；② 增强施加的**解码阶段 / 分辨率训练-推理处处同一步**（产品跑全分辨率、DINO 走 560 缩略图，直方图随分辨率变 → LUT 变）；③ 多视图的**双 CLS schema / 存储 / 端侧代价**；④ 参数（`ClipFactor=2.0` / `SaturationScale=1.0`）**冻结进模型契约**（DatasetBuilder 后缀 `+clhe{ClipFactor}ycc{SaturationScale}`），改则重提整库；⑤ 过线性探针 **≥80%（含影调对）**。

**量化判据（产出 `Training/probes/feature_probe.py`）：**

- 取 ~200 张含多组近重复 + 一组雾 / 低对比片的样本，入库拿 CLS（ViT-S/16@518，384d）。
- **第 0 步目视已搬进产品**：用控制栏 toggle 在真实雾片上直接切「原图 ↔ 增强」对比，确认"均衡 ≠ ACR 效果"（均衡多半放大噪声、显得生硬）→ 据此定出送进下面矩阵的候选算法（不必再在脚本里并排出图）。
- **矩阵**：输入制式{仅原片 / 仅增强 / 多视图} × 算法{产品内目视筛出的候选，如 CLHE / 去雾·CLAHE} × 集合{普通段内可比对（含影调对）/ 低对比雾片可比对}；在"人眼可分但星级不同"的段内可比对上，用逻辑回归 / 线性 SVM 对 CLS **差异向量**做二分类（判谁星级更高）。**统计效力**：可比对总量 **≥ 300 对**（200 张派生不够就补样本），否则 80% 阈值的置信区间太宽；实测准确率落在阈值 ±3% 内视为边界情形——加样本复测再判，不硬判过 / 不过。增强算法须与产品 toggle **同一份实现**（C# 产品侧与 Python 探针侧按一致性清单①的容差对齐，否则探针结论不迁移到产品 / 入库）。
- **基座判据**：最佳制式准确率 **≥ 80%** → ViT-S 够用；**< 80%** → 升 ViT-L（或 518→1024）复测，仍不行再排查特征本身（决定 plan-3-0 §1 决策 8 落点）。
- **制式决策规则**：
  - 仅原片在低对比上已够分 → **不增强**，收工。
  - 否则 → **多视图**（保 B 补 A，兼得的形态）。
  - **仅增强默认不选**——通常牺牲 B（把"天生好影调"和"被救回来的"拉成一样），除非探针显示它在"靠影调区分的对"上不掉点。
  - 全都没用 → 分辨率 / 特征问题，升 ViT-L，而非折腾预处理。
- 产出：`feature_probe.py` + 一页结论（矩阵准确率表 + t-SNE 目视：近重复是否聚簇、跨题材是否分离）。

**待你明确（后续补充后再执行）：**

- **具体增强算法与参数**：**已定** = CLHE + `ClipFactor=2.0`（见上"产品化落地"）；**自适应强度已否决**（逐图变化伤可学习性，改固定常量）。仅余可选项：若要"均匀加对比"的全局观感，可在 CLHE 后叠一条固定 S 曲线 / gamma（同样规律、待需要再加）；最终是否入模仍待探针。
- **产品化思路**：toggle 是否记忆每图状态、是否提供强度档位；多视图在推理期的代价（双 DINO pass、`photos.db` 是否存两路特征、端侧成本），以及是否全库都用多视图、还是仅对判定为低对比的片用。
- **探针样本**：雾 / 低对比片正反例各几张（你提供），并入上面 ~200 张。

- **GATE**：制式选定 + 基座达标 + 产品 toggle 上真实雾片目视确认增强可用 → 1.3；不达标 → 升 backbone 再来。

### 1.3 选子集 + 全量入库

- 按**题材规则**圈定训练 / 测试子集（plan-3-0 §1 决策 11；配比由你定，控泛化 / 特化）。开工前先确认：
  - 磁盘余量（两万张 patch ≈ 30GB）；
  - 库目录的**拍摄日期 / 拍摄段可恢复**（按段 / 日划分、滑窗都依赖它）。
- **入库视图数由 §1.2 制式结论决定**：增强视图 CLS 以带参数后缀的 `model_id`（如 `dinov3_vits16_f32_518_v1+clhe2.0`）存 `photo_features` 第二行——每指纹原片 + 增强两行，天然满足"参数冻结进模型契约、改参即整库失效"（§1.2 清单④）；patch token 仍只存原片一份（M4 空间权重以原片为准）。**M1 探针阶段两行都提**（`--no-enhance` 可关），是否最终入模由探针 + 制式决策定。
- **顺手写 `is_retouched` 标记**（photos 表加列，来源 = 1.1 的精修回溯匹配结果 → 清单 `retouchedList`；未提供则 NULL），供 M2 锚点 / 绝对池与金标准（§5）用。
- 跑 `Training/DatasetBuilder`（清单驱动、DirectML 加速）批量提取 → 填满**独立数据集库**（非产品 photos.db，可自由扩展、便携、不受查看器清库影响）。数据集库表/列名与产品对齐（Python 侧按 photos.db 写法可直接读），额外多训练列（增强行 / is_retouched / source_rel_path / event_label / subject_label / formats）+ `dataset_meta` 契约表。RAW 星级同时读同名 `.xmp` sidecar（承 §1.1）。
- **GATE**：工具产出覆盖率报告 + 判定（`<db>.coverage.md`）——指纹聚合正确（同次曝光 RAW/JPG/HEIF 合一）、EXIF / rating 覆盖率达标、四路（原片 CLS / 增强 CLS / patch / cv_grid）覆盖全部可解码组（仅 RAW 组无法解码，合法单列不计失败）。

### 1.4 data_audit（看清真实分布、校准所有阈值）

新建 `Training/audit/data_audit.py`，读数据集库输出（这是 M2+ 一切阈值的来源）：

| 统计项 | 用途 / 校准什么 |
|---|---|
| 星级分布直方图（总体 + 每段） | 确认锦标赛结构；验证"每段分布大致相同"（plan-3-0 §0.3） |
| CLS cosine 全库直方图 + 分位 | 校准相似度→权重映射、tie 上限 |
| 段内 vs 跨段 cosine 分布 | 验证滑窗 + 相似度能否分离可比对 |
| 拍摄段切分结果（按时间间隔 / 相似度） | 看每段张数、段时长，定窗口大小；**并核算 M2 人工偏移工作量**（段数 × 每段代表张数，几百段级别时人工评级是否可行，直接影响 plan-3-2 偏移法 vs 决策 3d 学习偏置的取舍） |
| `is_retouched` 覆盖率（按段 / 按好坏天气分桶） | 验证 plan-3-0 §0.4"覆盖不均"的实际形状，定精修信号的权重方式 |
| 近重复对（cosine ≥ 0.98）的 CV 距离分布 | 校准"条件保留"的 CV 差异阈值（决策 1） |
| EXIF / rating 字段覆盖率 | 确保 ≥ 95%，定缺失策略 |
| ≥3★ 实际占比 | 校准"Top 12.5%"是否对得上真实分布（不对则调召回口径） |

- **GATE（M1 总闸）**：星级可读 + 特征敏感 + 数据覆盖达标 + 分布无异常 → **M1 完成，进 plan-3-2（M2）**；任一异常 → 停下排查，不带病进训练。

### 1.5 绝对性探针（半天级，M1 附加产出）

> 章程自认"跨段绝对分是尽力外推、是核心不确定性"（plan-3-0 §0.6 挑战 3），但首次实测原本要等到 M4——这里用半天工作量把它提前两个里程碑。

- **做法**：人工标 **50–100 个跨段对**（不同天 / 不同段、绝对优劣人工可明确判断的两张，覆盖"好天平庸 vs 坏天最佳"这类关键构型），复用 §1.2 的 `feature_probe.py` 框架，对 CLS 差异向量测线性可分性。
- **用途**：提前量化"视觉特征泛化"（plan-3-0 决策 3c）这条腿的真实强度，直接决定 plan-3-2 里人工偏移法 (3a) vs 可学习段偏置 (3d) 的权重配比与 M2 的投入规模。
- **不设硬 GATE**：结果好 → 特征已携带绝对信号，偏移 / b_seg 只需轻量校准；结果差 → M2 偏移监督要加重、M5 大概率触发。无论好坏都有对应路线，但结论必须写进 data_audit 报告。

---

## 2. M1 产物

- 填满的**独立数据集库**（`Training/DatasetBuilder` 产出，选定子集，~万级，原片 + 增强 CLS 两路 + patch + cv_grid + EXIF + rating + `is_retouched` + 来源标签 + `dataset_meta` 契约）。
- 线性探针结论（backbone 是否够、是否需 ViT-L）。
- **绝对性探针结论**（§1.5，跨段绝对可分性 → plan-3-2 偏移法 vs 学习偏置的配比依据）。
- **查看器控制栏增强 toggle**（产品功能落地；同时是算法目视迭代的载体）。
- **输入制式结论**（仅原片 / 多视图 + 选定的确定性增强算法，产品与模型输入同源），据此定 1.3 入库视图数。
- `data_audit` 报告（分布图 + 一组**校准好的阈值 / 参数建议**，供 M2+ 用）。
- 拍摄段切分草案（供 M2 偏移法 + M3 配对复用）。

---

## 3. 后续里程碑（简述，随 M1 结果调整，细节不锁）

> 只记方向与 GATE，承 plan-3-0 §1 已锁定技术决策。三册**契约册已建立**（[plan-3-2 数据侧 = M2+M3](dinov3-photo-ranking-plan-3-2-calibration-pairs.md)、[plan-3-3 训练侧 = M4+M5](dinov3-photo-ranking-plan-3-3-training-baseline.md)、[plan-3-4 交付侧 = M6+M7](dinov3-photo-ranking-plan-3-4-integration-deploy.md)），各册目标 / 决策清单 / 输入 / 产物 / GATE 以册内契约为准；本节仅留一行级概览，原 §6 停车位内容已迁入各册。

- **M2 跨段绝对校准（偏移法）**：拍摄段切分 → 每段抽代表 + 精修锚点入"绝对池"跨组评级 → 拟合每段偏移 → 产出绝对星级（连续、不 clamp）。人工偏移 (3a) vs 可学习段偏置 (3d) 的配比按 §1.5 绝对性探针 + §1.4 工作量核算定（见 [plan-3-2 §5.1](dinov3-photo-ranking-plan-3-2-calibration-pairs.md) 备选）。**GATE**：偏移自洽（重复评级一致）+ 跨天"同绝对级"能对齐；否则退回纯相对、绝对留 v2。
- **M3 训练对生成**：段内对（高权，相似度加权 + 条件保留 + 大星级差降权）+ 跨段对（绝对星级派生、**低权**）；候选组为单位存储；按段 / 日划 split。
- **M4 基线 + 绝对涌现测量**：段内 ranking 立地基（段内 top-1、段内 0-5 准确率）；金标准 / 锚点测**绝对涌现**（好/坏天气片落位是否合理、锚点是否落顶端）→ **a/b/c 决策点**（绝对够 → 进 M6；不够 → M5）。失败模式→对策映射已提前锁定，见 [plan-3-3 §4](dinov3-photo-ranking-plan-3-3-training-baseline.md)。
- **M5 绝对校准增强（条件触发）**：绝对不够才加大偏移监督权重 / 补少量 targeted 跨段标注；重测绝对涌现。
- **M6 系统集成 + 全谱评估**：模型 + 去重压低算法 + **废片闸门**（plan-3-0 决策 14：CV / 规则先拦废片再进模型，验收含未删原始废片样例）+ 窗口召回；两条线评估（≥3★ 召回 + 0-5 准确率）+ 七类场景抽检 + 金标准集。**GATE**：达标 → M7，否则失败分析回退。
- **M7 部署**：ONNX 导出 + `PhotoViewer/Core/AI/` C# 推理集成（A2 已打通端侧链路，主要是换最终模型）。

---

## 4. M1 工具清单

| 工具 | 状态 | 职责 |
|---|---|---|
| `Training/DatasetBuilder/`（C#） | **已就绪** | 清单驱动：扫描 → 指纹聚合（RAW/HEIF/JPG 合一，含 `.xmp` sidecar 星级）→ EXIF + rating → DINO(原片 CLS + 增强 CLS + patch) → CV → **独立数据集库** + 覆盖率报告 + GATE。`--scan-only` 入库前分布探查（焦段/星级/拍摄时间跨度与粗切段数，不解码、不建库），供挑样本 / 核验批次 |
| `Tools/ExifTestTool/`（C#） | 已有 | 星级 / EXIF 可读性抽查（1.1） |
| 查看器控制栏增强 toggle（C#，`ViewModels/Main` 控制 + 图片 VM） | **已落地**（2026-07，CLHE 随产品上线） | 主画面原地切换原片 ↔ 确定性增强；既是产品功能又是算法探索沙盒（1.2） |
| `Training/probes/feature_probe.py` | **已建成**（首轮结论见 [../EXECUTION-LOG.md](../EXECUTION-LOG.md) 2026-07-18） | 增强制式量化判据：原片/增强/多视图 × 算法 × 集合的线性探针 + t-SNE（1.2）；增强算法与产品 toggle 同源；1.5 绝对性探针复用同一框架（跨段对可分性） |
| `Training/audit/data_audit.py` | **新建** | 分布统计 + 阈值校准报告（1.4） |

---

## 5. 并行长周期任务：金标准验证集（建议 M1 期间就开始）

M4 起的评估、a/b/c 决策都以它为准绳（plan-3-0 §0.6 称其"唯一可信标尺"），且只能人工精选、周期长——**别等到 M4 才攒**。

- **硬约束（承 plan-3-0 §1 决策 11）**：金标准样本所在**拍摄段必须划入 test（或至少排除出 train）**，否则 M4 绝对涌现测量被污染——攒集子时就按拍摄日记录归属，别等 M3 切 split 时才对账。
- 规模 ~50-100 组，按 plan-3-0 §0.5 七类场景分布。参考构成：
  - ~30 组**近重复**（每组 3-8 张，标出最佳 1-2 张）
  - ~20 组**一般场景**（标 ≥3★ 该保留哪些）
  - ~20 张**孤立片**（标是否值得进人工二审）
  - ~10 组**极相似**（标"不可分 / tie"）
  - 少量**未删原始废片**（来自下次外拍保留的 dump，见 §1.1；测废片闸门，plan-3-0 决策 14）
- 另含**精修精品锚点**（plan-3-0 §0.4）：既是金标准顶端，也是 M2 偏移法绝对池的成员。
- 这套集子上算的 0-5 准确率 / 组内 top-1 / 七类场景表现，比跑在带噪星级上的自动指标可信得多。

---

## 6. 后续里程碑的初期想法（已迁入各契约册）

> 三册契约册已建立（plan-3-0 §2），本节原有内容已迁入各册、此处只留指针；**新想法直接写入对应册中标注"未锁定"的想法节**，不再经停本文件。
>
> - 原 §6.1 M2 评分偏移法（含可学习段偏置备选）→ [plan-3-2 §5.1](dinov3-photo-ranking-plan-3-2-calibration-pairs.md)
> - 原 §6.2 M4 单图编码器架构 → [plan-3-3 §6.1](dinov3-photo-ranking-plan-3-3-training-baseline.md)
> - 原 §6.3 M4 失败模式→对策映射（已锁定）→ [plan-3-3 §4](dinov3-photo-ranking-plan-3-3-training-baseline.md)
