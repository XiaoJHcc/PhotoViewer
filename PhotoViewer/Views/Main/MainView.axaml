<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:PhotoViewer.ViewModels"
             xmlns:views="clr-namespace:PhotoViewer.Views"
             xmlns:fileViews="clr-namespace:PhotoViewer.Views.Main.File"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="PhotoViewer.Views.MainView"
             x:DataType="vm:MainViewModel">
    <Grid>
        <!-- 垂直布局（上中下）-->
        <Grid RowDefinitions="Auto,*,Auto,Auto" IsVisible="{Binding !IsHorizontalLayout}">
            <!-- 顶部：文件栏 -->
            <Border
                x:Name="TopThumbHost"
                Grid.Row="0"
                Margin="-100, -100, -100, 0"
                Padding="100, 100, 100, 0"
                Background="#111111"
                BorderBrush="#333333"
                BorderThickness="0,0,0,1"
                IsVisible="{Binding IsThumbnailViewVisible}">
                <fileViews:FileView x:Name="TopFileView" DataContext="{Binding FileVM}" />
            </Border>

            <!-- 中间：图片显示区域 -->
            <views:ImageView
                Grid.Row="1"
                Background="#000000"
                DataContext="{Binding ImageVM}" />

            <!-- 中间：细节预览 -->
            <Border
                Grid.Row="2"
                Margin="-100, 0, -100, 0"
                Padding="100, 0, 100, 0"
                Background="#111111"
                BorderBrush="#333333"
                BorderThickness="0,1,0,0"
                IsVisible="{Binding IsDetailViewVisible}">
                <views:DetailView DataContext="{Binding DetailVM}" />
            </Border>

            <!-- 底部：控制栏 -->
            <Border
                Grid.Row="3"
                Margin="-100, 0, -100, -100"
                Padding="100, 0, 100, 100"
                Background="#111111"
                BorderBrush="#333333"
                BorderThickness="0,1,0,0"
                IsVisible="{Binding IsControlViewVisible}">
                <views:ControlView DataContext="{Binding ControlVM}" />
            </Border>
        </Grid>

        <!-- 水平布局（左中右）-->
        <Grid ColumnDefinitions="Auto,*,Auto,Auto" IsVisible="{Binding IsHorizontalLayout}">
            <!-- 左侧：文件栏 -->
            <Border
                x:Name="LeftThumbHost"
                Grid.Column="0"
                Margin="-100, -100, 0, -100"
                Padding="100, 100, 0, 100"
                Background="#111111"
                BorderBrush="#333333"
                BorderThickness="0,0,1,0"
                IsVisible="{Binding IsThumbnailViewVisible}">
                <fileViews:FileView x:Name="LeftFileView" DataContext="{Binding FileVM}" />
            </Border>

            <!-- 中间：图片显示区域 -->
            <views:ImageView
                Grid.Column="1"
                Background="#000000"
                DataContext="{Binding ImageVM}" />

            <!-- 中间：细节预览 -->
            <Border
                Grid.Column="2"
                Margin="0, -100, 0, -100"
                Padding="0, 100, 0, 100"
                Background="#111111"
                BorderBrush="#333333"
                BorderThickness="1,0,0,0"
                IsVisible="{Binding IsDetailViewVisible}">
                <views:DetailView DataContext="{Binding DetailVM}" />
            </Border>

            <!-- 右侧：控制栏 -->
            <Border
                Grid.Column="3"
                Margin="0, -100, -100, -100"
                Padding="0, 100, 100, 100"
                Background="#111111"
                BorderBrush="#333333"
                BorderThickness="1,0,0,0"
                IsVisible="{Binding IsControlViewVisible}">
                <views:ControlView DataContext="{Binding ControlVM}" />
            </Border>
        </Grid>
    </Grid>
</UserControl>